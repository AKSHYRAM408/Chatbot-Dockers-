apiVersion: v1
kind: Service
metadata:
  name: chatbot-service
  labels:
    app: chatbot
spec:
  type: LoadBalancer
  selector:
    app: chatbot
  ports:
  - name: streamlit
    protocol: TCP
    port: 80
    targetPort: 8501
  - name: metrics
    protocol: TCP
    port: 8000
    targetPort: 8000
